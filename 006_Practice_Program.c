#include<stdio.h>
void main()
{
    int r=15;
    float pi=3.14;
    int dia=r+r;
    float distance=pi*dia;
    printf("Dist_travelled after one rotation of the wheel is %f having radius %d",distance,r);
}